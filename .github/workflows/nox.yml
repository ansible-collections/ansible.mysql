---
name: antsibull-nox

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  ansible-lint:
    name: Nox - Ansible Lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out collection
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Run ansible-lint
        uses: ansible-community/antsibull-nox@stable-1
        with:
          sessions: ansible-lint

# This test validate that the collection can be built and imported in Galaxy.
# It catches error before they are sent to Zuul, letting us fix mistakes prior to a release.
  galaxy-importer:
    name: Nox - Galaxy Import
    runs-on: ubuntu-latest
    steps:
      - name: Check out collection
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Run Galaxy importer
        uses: ansible-community/antsibull-nox@stable-1
        with:
          sessions: build-import-check

  sanity:
    name: Nox - Sanity
    runs-on: ubuntu-latest
    steps:
      - name: Check out collection
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Run sanity tests
        uses: ansible-community/antsibull-nox@stable-1
        with:
          sessions: ansible-test-sanity

  unit:
    name: Nox - Unit
    runs-on: ubuntu-latest
    strategy:
      # As soon as the first unit test fails,
      # cancel the others to free up the CI queue
      fail-fast: true
    steps:
      - name: Check out collection
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Run unit tests
        uses: ansible-community/antsibull-nox@stable-1
        with:
          sessions: ansible-test-units
